# The home page is two buttons. Nothing else.
# `hero.pitch` is still read by head.html as the meta description fallback.

hero:
  pitch: "Posts, and the company."

links:
  - label: "Posts"
    url: "/posts/"
    primary: true
  - label: "intentius.io"
    url: "https://intentius.io"
